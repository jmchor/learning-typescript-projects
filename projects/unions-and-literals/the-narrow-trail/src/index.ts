export function runCommands() {
	let food = 5;
	let water = 5;
	let availableRessource: "food" | "water" | undefined;

	let diceRoll: number = Math.floor(Math.random() * 6) + 1;
	// Declare your variables and runtime logic here! ✨
}
